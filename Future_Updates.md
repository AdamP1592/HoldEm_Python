# Future Updates:

- Now that the memory leak is resolved. In training loop have the networks get trained asynchronously.

- Combine both new and old weighted models for the demo for more variance in strategy.

- Modify dqn framework when training from a file to include the current